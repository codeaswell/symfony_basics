{% extends 'base.html.twig' %}

{% block body %}

{% if title != 'bite' %}
    <h1>{{ title }}</h1>
{% endif %}

<h2>Nombre d'items : {{ comments|length }}</h2>

<ul>
    {% for comment in comments %}
        <li>{{ comment }}</li>
    {% endfor %}
</ul>

{% endblock %}
